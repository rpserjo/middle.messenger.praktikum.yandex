<div class="messages">

    <div>
        <div id="intersector"></div>
        {{#if canLoadMore}}
            {{{ loadMore }}}
        {{/if}}
    </div>
    <div class="messages__list">
        {{#each messages}}
            {{{ this }}}
        {{/each}}
    </div>

</div>
